{
    "name": "karaokenatin",
    "version": "0.2.0-beta",
    "private": true,
    "description": "LAN karaoke system: Tauri host app with an embedded web server and browser-based guest remotes",
    "license": "MIT",
    "scripts": {
        "setup": "pnpm install && pnpm run build:shared",
        "build:shared": "cd packages/shared && pnpm run build",
        "dev:host": "pnpm run build:shared && cd apps/host && pnpm run tauri:dev",
        "build:host": "pnpm run build:shared && cd apps/host && pnpm run build && pnpm run tauri build",
        "build": "pnpm run build:host",
        "typecheck": "pnpm run build:shared && cd apps/host && npx tsc --noEmit",
        "test": "vitest run",
        "test:watch": "vitest",
        "lint:rust": "cd apps/host/src-tauri && cargo clippy --all-targets -- -D warnings",
        "fmt:rust": "cd apps/host/src-tauri && cargo fmt",
        "check": "pnpm run typecheck && pnpm run test"
    },
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "devDependencies": {
        "vitest": "^2.1.9"
    }
}
